# 后续工作

## P1：角色与好友记忆试用

- 重启桌面程序，在「角色 / 好友记忆」设置风格、创建一个好友、导入少量历史，分析后查看依据。
- 验收：角色措辞符合预期，事实不被模型改写，切换好友不串上下文；观察过时后能修正或清除。
- 尚需真实历史质量对照，当前线上验证仅使用合成资料。

## P1：实时采集仍受阻

- wxauto 可安装并加载，但 Plus 未激活；免费版支持上限低于本机微信版本。
- 下一步需有效授权后验证指定会话的只读消息与身份；或者另行实现自动 OCR 采集。
- 不将 MCP 依赖加载成功当作消息读取成功；当前没有启用全微信监听。

## P1：桌面版真实微信试用（当前优先）

- 用户运行本地 `desktop/start-local.cmd`，按 [桌面说明](desktop/README.md) 完成一次单聊截图、
  OCR 核对、回复生成、复制。依赖本机三个 API 配置和联网，已复用已有 `.env`。
- 验收：框选与预览一致，发言方向可核对，清空后旧结果不返回，复制不触发微信发送。
- 待验证：主屏/副屏、Windows 缩放、深色气泡、多行消息、真实 OCR 耗时与建议质量。
- 界面视觉验收受本机控制工具截图接口错误阻塞，需人工反馈或修复工具后验证。
- 后续再评估自动采集 / MCP；需要可靠的当前会话身份和切换检测后才能减少人工核对。

## P1：用户真机反馈

- 用户安装试用版并配置判断/回复接口，按 [试用手册](docs/runbooks/android-trial.md) 验证。
- 验收：会话切换不串结果；新消息可继续分析；草稿不被覆盖；从不自动发送。
- 依赖：Android 11+ arm64 手机、可访问的模型接口；记录手机与微信版本。

## P2：后续改进（未实施）

- 对更多微信/系统版本建立采集回归样本，改善同名联系人和群聊识别。
- 消息时间戳/唯一 ID 不可用时完善历史记录策略，避免把未见过的旧屏当新消息。
- 建立独立对话评测集，评估误判、候选质量和延迟，不复用调参集作为独立验证。
- 改善国产系统后台保活；不要把当前试用结果推断为长期稳定性。
- 按实际风险逐步处理当前 27 项 lint 警告，不在本次试用前盲目升级依赖或 SDK。
